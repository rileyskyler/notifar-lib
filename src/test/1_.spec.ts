import 'mocha';
import { expect, assert } from 'chai';

import { init } from '../App'



describe('Test', () => {

    before('before', () => {

        console.log('tt')
    })

    it('should', () => {
        
    })
})