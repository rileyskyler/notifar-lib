import { expect, assert } from 'chai';



import 'mocha';

describe('Test', () => {

    before('before', () => {

            // mongoose.connection.db.createCollection('users', (err: any, res: any) => {
    //     console.log(err, res)
    // })

        console.log('tt')
    })

    it('should', () => {
        
    })
})